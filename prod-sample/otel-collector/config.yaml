receivers:
  otlp:
    protocols:
      grpc:
exporters:      
  logging:
    verbosity: detailed
service:
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [logging]
    # traces:
    #   receivers: [otlp]
    #   exporters: [logging]
