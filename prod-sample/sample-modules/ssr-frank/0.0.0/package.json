{
  "name": "ssr-frank",
  "version": "0.0.0",
  "description": "A module to validate doing 'stuff' (could be anything) on the server prior to rendering.",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "bundle-module",
    "clean": "rimraf build",
    "prepare": "npm run build"
  },
  "dependencies": {
    "immutable": "^4.0.0",
    "prop-types": "^15.8.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-helmet": "^5.2.1"
  },
  "devDependencies": {
    "@americanexpress/one-app-bundler": "^7.0.0-rc.8",
    "babel-preset-amex": "^4.0.0",
    "rimraf": "^3.0.2"
  }
}
